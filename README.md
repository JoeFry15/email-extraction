# Email Extraction

Reads a text file and extracts information about the email addresses within the file.

Counts specified email addresses in several ways. Creates an object that adds email domains and counts the number of times they appear.


